# Release evidence verification — 2026-09-04

## Outcome: local release evidence approved

The source-current five-format capture after the XLSX identity-entry change records
`approved: true` and `ready: true`. XLSX bootstrap p95 is **432.5 ms < 500 ms**
(67.5 ms headroom), and Markdown small-file open p95 is **14.3 ms < 20 ms**.
No fixed ceiling, sample minimum, visual reference or mask was relaxed.

- Capture completed: `2026-09-04T02:51:06.592Z`.
- Source SHA-256: `e51861c2bb32442844952afd430dd4024e11052eda6ec3af5c17359fddb47f9a`.
- Host: macOS arm64, Apple M4, 10 logical CPUs, 16 GiB RAM.
- Browser: Playwright Chromium `140.0.7339.16`; editor 720 × 900, scale factor 1.
- Every format: 7 cold starts, 7 opens **per** small/medium/large fixture, 21 interactions.
- Each format runs in a fresh browser process. No unit/browser suite was run in
  parallel with the canonical performance capture. Other user applications were
  not stopped; this is evidence for the recorded local host, not every OS/device.

## Measurements

Times below are nearest-rank p95 in milliseconds. Full summaries, fixture hashes,
memory measurements, phase breakdowns and visual artifact hashes are in the JSON.

| Format | Cold start | Open small | Open medium | Open large | Interaction | Visual difference |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| DOCX | 297.057 | 127.5 | 131.1 | 274.3 | 4.9 | 0.1% |
| Markdown | 223.242 | 14.3 | 58.2 | 320.4 | 2.6 | 0.5% |
| XLSX | 852.143 | 50.1 | 273.5 | 333.7 | 35.2 | 0.1% |
| PPTX | 331.720 | 12.7 | 29.9 | 100.9 | 24.4 | 0.1% |
| PDF | 389.624 | 105.9 | 119.4 | 136.7 | 33.6 | 0.5% |

All five visual comparisons pass the existing 3% limit with no masks. XLSX total
cold start passes its 1,400 ms limit. Bootstrap median is 420.2 ms and p95 is
432.5 ms; module-graph p95 is 361.6 ms.
The capture's other non-fixed ceilings are **unapproved suggestions** generated by
the existing p95-plus-20% rule. The previous approved JSON was absent; historical
documentation is not a substitute for recovering or reviewing those exact budgets.

## XLSX startup experiment and TDD

Two changes affect only TandemFolio-owned packaging and the browser adapter:

1. Build the embedded modules' static dependency index at packaging time. Inflate
   dependency-free modules directly to a UTF-8 Blob, avoiding a large runtime
   string decode, dependency scan and Blob re-encode. Keep all lazy modules and
   their cached module identity inside the same self-contained resource.
2. Load the ZIP/XML package adapter with `import()` inside the existing async
   `openBuffer`. The native blank worksheet needs no package parsing. First-open
   work remains measured in open-document timing; no renderer is replaced.
3. Keep the critical entry as identity-encoded text in the vault. The vault creates
   its single importable Blob URL without startup base64 or gzip work, so lazy
   chunks can still import the entry's shared exports without a second evaluation.

RED/GREEN: the packaged graph test failed without metadata, then passed; the
on-demand package-I/O test failed while ZIP/XML stayed in the entry, then passed.
A real-browser regression checks concurrent lazy locale imports share the same
module and retain the visible, once-mounted worksheet. Existing open/save/reopen
tests cover the deferred package adapter. No preset, locale, operation, undo path,
startup timing boundary, fixed budget or sample requirement was removed/changed.
For the identity-entry slice, the first data-URL implementation passed all static
package assertions but timed out all three focused browser tests; three isolated
cold repros measured bootstrap at 650.9–699.0 ms. The tightened identity-entry
contract failed first, then the minimal vault change passed 13/13 package tests,
3/3 focused browser tests, and the full 72-case XLSX browser matrix.

| Capture | Scope | Bootstrap median | Bootstrap p95 | Small open p95 |
| --- | --- | ---: | ---: | ---: |
| Before changes | Full five formats | 489.7 | 512.2 | 40.7 |
| Byte path only | XLSX candidate | 491.6 | 510.6 | — |
| Byte path + deferred package I/O | XLSX candidate | 468.6 | 487.6 | 54.1 |
| Same changes, canonical recapture | Full five formats | 477.2 | 521.4 | 50.0 |
| Direct data-URL entry | Three cold repros | 650.9–699.0 | — | — |
| Identity entry, first candidate | XLSX candidate | 421.0 | 423.5 | 66.9 |
| Identity entry, repeat candidate | XLSX candidate | 417.5 | 422.7 | 47.4 |
| Identity entry, approved capture | Full five formats | 420.2 | 432.5 | 50.1 |

All candidate rows use seven cold starts except the explicitly labelled three-run
data-URL repro. The two independent identity candidates agree within 0.8 ms at p95;
the complete approved capture remains below the fixed ceiling with 67.5 ms headroom.
The failed data-URL experiment is not retained. First-open work remains visible in
the open-document columns rather than being shifted outside the measured contract.

The [pre-change bundle](before-xlsx-bootstrap.tar.gz) preserves the previous
JSON, actual images, source manifest and inherited references. The
[targeted candidates](xlsx-startup-candidates.tar.gz) preserve both diagnostic
JSON/image sets at their original relative paths. These archives are historical
evidence, not plugin distribution packages or current release authority.
The two passing identity candidates and their visual artifacts are preserved in
[identity candidates](xlsx-identity-candidates.tar.gz); the approved five-format
JSON remains the sole current release authority.

The rebuilt XLSX HTML is 12,818,430 bytes (budget 21,000,000); its rewritten entry
inflates to 9,944,299 bytes (budget 11,000,000). All 197 embedded modules have
resolvable static dependency metadata. The post-build source fingerprint still
matches the canonical capture; rebuilding did not invalidate the evidence.

## Verification and TDD changes

- Restored the upstream source/visual manifest and retained the existing 28 PNG
  references by removing their accidental ignore rule. No snapshots were updated.
  See [visual review](visual-review.md) for their provenance and limitations.
- RED: `capture --describe` failed when the visual manifest was missing. GREEN:
  defer manifest loading until actual capture; the CLI regression now executes in
  an isolated checkout with no release files.
- RED: `upstream:check` found the configured source pin missing from `AGENTS.md`.
  GREEN: record the existing pin, without changing it or any product rule.
- RED: PDF/XLSX recovery tests counted the newly required clean-import checkpoint
  as the first edit. GREEN: await that checkpoint, then require exactly one more
  per dirty version and no repeat after the recovery interval.
- RED: the capabilities integration test required unconditional `ready: true` even
  with failed evidence. GREEN: assert the public capability response follows the
  generated all-format decision. The release gate and archiver remain fail-closed;
  this changes a stale test expectation, not release authority or product behavior.
- Release CLI/provenance/gate tests: 20 passed. Distribution tests: 5 passed.
- Post-optimization `npm run check` passed end to end: upstream configuration,
  operation manifest, all-workspace typecheck, and all workspace unit/integration
  suites passed (**3,445 passed, one pre-existing skipped test**, including
  MCP-server 383/383); release gate, five-format build, resource ceilings,
  packaged MCP smoke (25 tools), and dependency licenses also passed.
- Before the identity-entry slice, the full browser run passed **176/176** in
  7.9 minutes. Against the rebuilt identity-entry resource, all **72/72** XLSX
  community browser cases passed in 8.8 minutes, and the three focused startup,
  lazy-module, and offscreen-resume cases passed separately. These cover real
  open/save/reopen, native Ribbon and MCP operations, shared undo, package
  structure, and the single mounted runtime. No snapshot was updated.
- The rebuilt source fingerprint still equals the approved evidence fingerprint.
  No release archive was created.
- In the earlier verification, DOCX password protection timed out while suites
  ran concurrently; isolated rerun passed without code/timeout changes. The current
  full unit run passed and was not run concurrently with the browser suite.
- The source fingerprint, all ten baseline/current PNG hashes, and every format's
  actual 7/7/21 measurement counts were independently checked after capture.
- `release:gate` exits 0 with `ready: true` and writes an all-format true projection.

## Remaining release work

Keep the release source frozen and ensure the final build retains fingerprint
`e51861c2bb32442844952afd430dd4024e11052eda6ec3af5c17359fddb47f9a`.
The Linux package and Windows/macOS native installation jobs still require a real
GitHub Actions run; local macOS checks do not certify Windows. No commit, tag,
push, GitHub Release, or plugin marketplace publication was performed here.
