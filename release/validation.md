# Release evidence verification — 2026-09-10

## Current status: beta release evidence approved

The source-current five-format capture records `approved: true` and `ready: true`.
XLSX bootstrap p95 is **480.6 ms < 500 ms** (19.4 ms headroom), total cold-start
p95 is **849.358 ms < 1,400 ms**, and its identity entry is 7,856,180 bytes
under the unchanged 11,000,000-byte ceiling. No fixed ceiling, sample minimum,
visual reference, or mask was relaxed.

- Capture completed: `2026-09-10T02:27:39.577Z`.
- Source fingerprint: `9ecbcd09b8f20c0692f184f97d1a72ed9ad3bce499e0d2e99748fb541563f42b`.
- Host: macOS arm64, Apple M4, 10 logical CPUs, 16 GiB RAM.
- Browser: Playwright Chromium `140.0.7339.16`; editor 720 × 900, scale factor 1.
- Every format: 21 cold starts, 21 opens per small/medium/large fixture, 21 interactions.
- All five visual comparisons pass the unchanged 3% limit without masks; the
  reviewed screenshots show each retained editor surface mounted and nonblank.

Current packaged raw/gzip sizes are DOCX 3,519,897/990,721, Markdown
2,573,562/936,420, XLSX 8,734,947/6,475,283, PPTX 3,891,323/1,146,141,
and PDF 6,722,930/3,396,388 bytes. All are below their existing resource ceilings.

## Current measurements

Times below are nearest-rank p95 in milliseconds. Full summaries, fixture hashes,
memory measurements, phase breakdowns and visual artifact hashes are in the JSON.

| Format | Cold start | Open small | Open medium | Open large | Interaction | Visual difference |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| DOCX | 339.146 | 83.4 | 150.1 | 335.6 | 5.3 | 0.7% |
| Markdown | 282.473 | 14.8 | 67.3 | 394.5 | 3.0 | 0.6% |
| XLSX | 849.358 | 71.9 | 162.6 | 416.6 | 35.3 | 1.1% |
| PPTX | 346.261 | 111.0 | 135.2 | 227.5 | 24.7 | 0.1% |
| PDF | 420.720 | 112.5 | 119.5 | 137.9 | 34.5 | 1.2% |

All five visual comparisons pass the existing 3% limit with no masks. XLSX total
cold start passes its 1,400 ms limit. Bootstrap p95 is 480.6 ms; its p95
decomposition is resource receive 28.6 ms, module graph 445.9 ms, and React mount
9.7 ms.
The capture's other non-fixed ceilings are **unapproved suggestions** generated by
the existing p95-plus-20% rule. The previous approved JSON was absent; historical
documentation is not a substitute for recovering or reviewing those exact budgets.

## 2026-09-08 XLSX blank-entry optimization and TDD

The XLSX blank-editor entry no longer evaluates the EMF/WMF converter or command-only dialog
surfaces. Metafile decoding remains embedded and loads when workbook media requires it. Formatting,
pivot, formula, page-layout, advanced-filter, chart, equation, screenshot, symbol, and icon dialogs
remain embedded and load when their existing command state opens. A UI-independent symbol helper
keeps Agent and user insertion on the same mounted cell mutation.

RED/GREEN package tests require the metafile converter and 17 named dialog chunks to remain outside
the initial entry but inside the complete self-contained graph. Mounted browser regressions cover
media save/reopen and representative deferred dialogs. The entry fell from about 8.40 MB to
7,860,540 bytes. The complete Sheets suite passes 1,991 tests with one designed skip; the approved
21-sample capture records bootstrap p95 450.9 ms under the unchanged 500 ms ceiling.

## 2026-09-07 XLSX startup-block fix and TDD

The packaged Blob module previously awaited a deferred locale import during module
evaluation and could stall forever with a workbook installed but no canvas. The
locale catalog now loads from `LocaleProvider` after mount and increments a catalog
revision when ready. Required font aliases remain complete before Univer's first
measurement, while the App graph is statically linked into the identity entry and
the operation registry loads on first command dispatch. This removes the circular
Blob-module wait and avoids a second 8.39 MB Blob creation/import without changing
renderer authority or retained languages.

RED/GREEN coverage first reproduced the packaged startup and module-graph failure,
then requires a sub-11 MB identity entry with no startup App chunk, preserved lazy
locale/package modules, and a real staged workbook open. The focused real-browser
suite passes 3/3, the complete XLSX unit suite passes 1,990 assertions in 174 files
with one designed skip, and the seven-sample XLSX candidate records bootstrap p95
491.9 ms before the current approved all-format capture records 467.6 ms.

## 2026-09-07 hidden-renderer command settlement fix

A captured Codex task showed a correct 9×8 XLSX range write remaining active for more than a minute
after an earlier malformed matrix had been rejected. The mounted renderer had received the command
from an already-issued long poll, then waited only for `requestAnimationFrame`; an occluded MCP App
can suspend that callback indefinitely, preventing ACK and making a replayed view's cooperative
handoff fail because the Broker still owns an active command.

The shared renderer-commit boundary now uses the animation frame when available and a 250 ms timer
fallback when painting is suspended. `xlsx.range.set_values` also rejects matrices whose height or
width differs from the target range before calling Univer. RED/GREEN coverage includes the complete
Host Bridge ACK chain with a permanently suspended animation frame, a malformed-then-valid XLSX
registry sequence, and a packaged Playwright case that receives and acknowledges a range command
while the XLSX canvas is offscreen. The full root run passes 4,904 assertions with one designed skip;
Host Bridge passes 49/49, XLSX passes 1,990 plus one skip, and MCP Server passes 392/392.

## 2026-09-08 blank XLSX stylesheet/theme save repair

Task `01a07e95-a365-7e30-bc21-1e0916b5aacb` reached revision 123 with six sheets,
validation, conditional formatting, notes, styles, number formats, and two charts. Save then failed
with `The workbook stylesheet is missing; formatting cannot be saved`. The same package rejected
`xlsx.document.set_theme` because it had no editable theme part. The blank/CSV package generator now
includes standard `styles.xml` and `theme1.xml` parts with their relationships and content types.
The browser package adapter repairs pre-R6-20 blank packages when exact recovery reopens them.

RED/GREEN coverage first reproduced the missing-stylesheet save path, then requires a blank workbook
to add a sheet, data, a native chart, named style, currency number format, and Office theme before a
successful save. The saved ZIP must contain the chart, stylesheet, and theme. The complete XLSX suite
passes 1,991 tests with one designed skip; a 14-sample diagnostic records bootstrap p95 at 494.9 ms,
and the approved five-format capture records 432.0 ms without changing any fixed ceiling.

## Historical 2026-09-04 XLSX startup experiment

Two changes affect only TandemFolio-owned packaging and the browser adapter:

1. Build the embedded modules' static dependency index at packaging time. Inflate
   dependency-free modules directly to a UTF-8 Blob, avoiding a large runtime
   string decode, dependency scan and Blob re-encode. Keep all lazy modules and
   their cached module identity inside the same self-contained resource.
2. Load the ZIP/XML package adapter with `import()` inside the existing async
   `openBuffer`. The native blank worksheet needs no package parsing. First-open
   work remains measured in open-document timing; no renderer is replaced.
3. Keep the critical entry as identity-encoded text in the vault. The vault creates
   its single importable Blob URL without startup base64 or gzip work, so lazy
   chunks can still import the entry's shared exports without a second evaluation.

RED/GREEN: the packaged graph test failed without metadata, then passed; the
on-demand package-I/O test failed while ZIP/XML stayed in the entry, then passed.
A real-browser regression checks concurrent lazy locale imports share the same
module and retain the visible, once-mounted worksheet. Existing open/save/reopen
tests cover the deferred package adapter. No preset, locale, operation, undo path,
startup timing boundary, fixed budget or sample requirement was removed/changed.
For the identity-entry slice, the first data-URL implementation passed all static
package assertions but timed out all three focused browser tests; three isolated
cold repros measured bootstrap at 650.9–699.0 ms. The tightened identity-entry
contract failed first, then the minimal vault change passed 13/13 package tests,
3/3 focused browser tests, and the full 72-case XLSX browser matrix.

| Capture | Scope | Bootstrap median | Bootstrap p95 | Small open p95 |
| --- | --- | ---: | ---: | ---: |
| Before changes | Full five formats | 489.7 | 512.2 | 40.7 |
| Byte path only | XLSX candidate | 491.6 | 510.6 | — |
| Byte path + deferred package I/O | XLSX candidate | 468.6 | 487.6 | 54.1 |
| Same changes, canonical recapture | Full five formats | 477.2 | 521.4 | 50.0 |
| Direct data-URL entry | Three cold repros | 650.9–699.0 | — | — |
| Identity entry, first candidate | XLSX candidate | 421.0 | 423.5 | 66.9 |
| Identity entry, repeat candidate | XLSX candidate | 417.5 | 422.7 | 47.4 |
| Identity entry, approved capture | Full five formats | 420.2 | 432.5 | 50.1 |

All candidate rows use seven cold starts except the explicitly labelled three-run
data-URL repro. The two independent identity candidates agree within 0.8 ms at p95;
the complete approved capture remains below the fixed ceiling with 67.5 ms headroom.
The failed data-URL experiment is not retained. First-open work remains visible in
the open-document columns rather than being shifted outside the measured contract.

The [pre-change bundle](before-xlsx-bootstrap.tar.gz) preserves the previous
JSON, actual images, source manifest and inherited references. The
[targeted candidates](xlsx-startup-candidates.tar.gz) preserve both diagnostic
JSON/image sets at their original relative paths. These archives are historical
evidence, not plugin distribution packages or current release authority.
The two passing identity candidates and their visual artifacts are preserved in
[identity candidates](xlsx-identity-candidates.tar.gz); the approved five-format
JSON remains the sole current release authority.

The rebuilt XLSX HTML is 12,818,430 bytes (budget 21,000,000); its rewritten entry
inflates to 9,944,299 bytes (budget 11,000,000). All 197 embedded modules have
resolvable static dependency metadata. The post-build source fingerprint still
matches the canonical capture; rebuilding did not invalidate the evidence.

## Verification and TDD changes

- The real-host Playwright gate now uses one worker. Its lifecycle cases restart
  the shared Broker, so cross-file execution could cancel another format's file
  picker or leave XLSX save/undo controls disabled. The fixed per-test timeout
  remains unchanged; serialization removes the shared-state race instead of
  hiding it with a larger timeout.
- RED: a capture launched by the macOS Codex app inherited background scheduling
  and pushed XLSX bootstrap beyond its fixed ceiling. GREEN: the release capture
  process removes only that inherited background policy with `taskpolicy -B` on
  macOS; other platforms remain unchanged. The approved 21-sample capture passes
  the original 500 ms bootstrap ceiling without discarding samples.
- Restored the upstream source/visual manifest and retained the existing 28 PNG
  references by removing their accidental ignore rule. No snapshots were updated.
  See [visual review](visual-review.md) for their provenance and limitations.
- RED: `capture --describe` failed when the visual manifest was missing. GREEN:
  defer manifest loading until actual capture; the CLI regression now executes in
  an isolated checkout with no release files.
- RED: `upstream:check` found the configured source pin missing from `AGENTS.md`.
  GREEN: record the existing pin, without changing it or any product rule.
- RED: PDF/XLSX recovery tests counted the newly required clean-import checkpoint
  as the first edit. GREEN: await that checkpoint, then require exactly one more
  per dirty version and no repeat after the recovery interval.
- RED: the capabilities integration test required unconditional `ready: true` even
  with failed evidence. GREEN: assert the public capability response follows the
  generated all-format decision. The release gate and archiver remain fail-closed;
  this changes a stale test expectation, not release authority or product behavior.
- Release CLI/provenance/gate tests: 20 passed. Distribution tests: 5 passed.
- The current release-host run passes **180/180** real-browser cases in 12.8
  minutes with one worker and no retries. The four XLSX cases that had failed
  intermittently in a prior long run also passed 12/12 across three focused
  repetitions before the final complete run.
- Post-optimization `npm run check` passed end to end: upstream configuration,
  operation manifest, all-workspace typecheck, and all workspace unit/integration
  suites passed (**4,904 passed, one designed skipped test**, including
  MCP-server 392/392); release gate, five-format build, resource ceilings,
  packaged MCP smoke (26 tools), and dependency licenses also passed.
- Before the identity-entry slice, the full browser run passed **176/176** in
  7.9 minutes. Against the rebuilt identity-entry resource, all **72/72** XLSX
  community browser cases passed in 8.8 minutes, and the three focused startup,
  lazy-module, and offscreen-resume cases passed separately. These cover real
  open/save/reopen, native Ribbon and MCP operations, shared undo, package
  structure, and the single mounted runtime. No snapshot was updated.
- The rebuilt source fingerprint still equals the approved evidence fingerprint.
  The local `v0.1.0-beta.2` ZIP and tar.gz archives were generated, both outer
  SHA-256 checks pass (`11259d1cecf4d9dab76cc5ad7abb4d0396af04b8e3685026c00b2c24af38e782`
  and `c51d08afbd4e072327c07df0fcf74e2bdfaf4c5b5ecd45c20c4471827e94b40b`),
  and the staged package verifier passes on Node.js 22.22.0.
- In the earlier verification, DOCX password protection timed out while suites
  ran concurrently; isolated rerun passed without code/timeout changes. The current
  full unit run passed and was not run concurrently with the browser suite.
- The source fingerprint, all ten baseline/current PNG hashes, and every format's
  actual 7/7/21 measurement counts were independently checked after capture.
- `release:gate` exits 0 with `ready: true` and writes an all-format true projection.

## Remaining release work

Keep the release source frozen and ensure the final build retains fingerprint
`9ecbcd09b8f20c0692f184f97d1a72ed9ad3bce499e0d2e99748fb541563f42b`.
The Windows/macOS native installation jobs still require a real GitHub Actions
run; local macOS checks do not certify Windows. The release tag workflow must
pass those platform jobs before its GitHub prerelease is treated as published.
