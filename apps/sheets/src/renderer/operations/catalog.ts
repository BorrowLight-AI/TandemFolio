import type { OperationCatalog } from '@tandemfolio/operation-contract'
import { ADDABLE_SHAPE_TYPES } from '../../shared/shape-types'

export const xlsxTableStyles = [
  'TableStyleLight1',
  'TableStyleLight9',
  'TableStyleMedium2',
  'TableStyleMedium4',
  'TableStyleMedium7',
  'TableStyleDark2',
] as const

export const xlsxStructuralCountSchema = {
  type: 'integer',
  minimum: 1,
  maximum: 10_000,
} as const

const xlsxColumnMutationInputSchema = {
  type: 'object',
  properties: {
    sheet: { type: 'string' },
    column: { type: 'string' },
    count: xlsxStructuralCountSchema,
  },
  required: ['sheet', 'column', 'count'],
  additionalProperties: false,
} as const

const xlsxColumnMutationOutputSchema = {
  type: 'object',
  properties: {
    sheet: { type: 'string' },
    column: { type: 'string' },
    count: { type: 'integer', minimum: 1 },
  },
  required: ['sheet', 'column', 'count'],
  additionalProperties: false,
} as const

const xlsxRowMutationInputSchema = {
  type: 'object',
  properties: {
    sheet: { type: 'string' },
    row: { type: 'integer', minimum: 1 },
    count: xlsxStructuralCountSchema,
  },
  required: ['sheet', 'row', 'count'],
  additionalProperties: false,
} as const

const xlsxRowMutationOutputSchema = {
  type: 'object',
  properties: {
    sheet: { type: 'string' },
    row: { type: 'integer', minimum: 1 },
    count: { type: 'integer', minimum: 1 },
  },
  required: ['sheet', 'row', 'count'],
  additionalProperties: false,
} as const

export const xlsxOperationCatalog = {
  format: 'xlsx',
  operations: [
    {
      id: 'xlsx.cell.set_value',
      format: 'xlsx',
      family: 'cell',
      summary: 'Set one scalar value in an XLSX cell.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          address: { type: 'string' },
          value: { type: ['string', 'number', 'boolean', 'null'] },
        },
        required: ['sheet', 'address', 'value'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          sheet: { type: 'string' },
          range: { type: 'string' },
        },
        required: ['changed', 'sheet', 'range'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.cell.set_formula',
      format: 'xlsx',
      family: 'cell',
      summary: 'Set one explicit formula in an XLSX cell.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          address: { type: 'string' },
          formula: { type: 'string' },
        },
        required: ['sheet', 'address', 'formula'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          sheet: { type: 'string' },
          range: { type: 'string' },
          formula: { type: 'string' },
        },
        required: ['changed', 'sheet', 'range', 'formula'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.chart.add',
      format: 'xlsx',
      family: 'chart',
      summary: 'Add one chart from an explicit bounded XLSX data range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          dataRange: { type: 'string' },
          type: {
            type: 'string',
            enum: ['column', 'bar', 'line', 'area', 'pie', 'doughnut', 'scatter', 'radar', 'combo'],
          },
          anchorCell: { type: 'string' },
        },
        required: ['sheet', 'dataRange', 'type'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          dataRange: { type: 'string' },
          type: { type: 'string' },
          anchorCell: { type: 'string' },
          visualId: { type: 'string' },
        },
        required: ['sheet', 'dataRange', 'type', 'visualId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.pivot.add',
      format: 'xlsx',
      family: 'pivot',
      summary: 'Create one native PivotTable from explicit source and target ranges.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          targetSheet: { type: 'string' },
          targetCell: { type: 'string' },
          name: { type: ['string', 'null'] },
          rowFields: {
            type: 'array',
            items: { type: 'string' },
            minItems: 1,
            maxItems: 8,
          },
          columnFields: {
            type: 'array',
            items: { type: 'string' },
            minItems: 0,
            maxItems: 8,
          },
          pageFields: {
            type: 'array',
            items: { type: 'string' },
            minItems: 0,
            maxItems: 4,
          },
          values: {
            type: 'array',
            items: {
              type: 'object',
              properties: {
                field: { type: 'string' },
                aggregation: {
                  type: 'string',
                  enum: ['sum', 'count', 'average', 'max', 'min'],
                },
                numberFormat: { type: ['string', 'null'] },
                showAs: {
                  type: ['string', 'null'],
                  enum: ['percentOfTotal', 'percentOfRow', 'percentOfCol', null],
                },
              },
              required: ['field', 'aggregation', 'numberFormat', 'showAs'],
              additionalProperties: false,
            },
            minItems: 1,
            maxItems: 8,
          },
        },
        required: [
          'sourceSheet',
          'sourceRange',
          'targetSheet',
          'targetCell',
          'name',
          'rowFields',
          'columnFields',
          'pageFields',
          'values',
        ],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          targetSheet: { type: 'string' },
          targetCell: { type: 'string' },
          name: { type: 'string' },
        },
        required: ['sourceSheet', 'sourceRange', 'targetSheet', 'targetCell', 'name'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.pivot.add_chart',
      format: 'xlsx',
      family: 'pivot',
      summary: 'Create one persisted chart from an explicitly identified XLSX PivotTable.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          pivotId: { type: 'string' },
          type: {
            type: 'string',
            enum: ['column', 'bar', 'line', 'pie', 'doughnut', 'radar'],
          },
        },
        required: ['pivotId', 'type'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          pivotId: { type: 'string' },
          type: {
            type: 'string',
            enum: ['column', 'bar', 'line', 'pie', 'doughnut', 'radar'],
          },
          chartId: { type: 'string' },
          truncated: { type: 'boolean' },
        },
        required: ['pivotId', 'type', 'chartId', 'truncated'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.pivot.refresh',
      format: 'xlsx',
      family: 'pivot',
      summary: 'Refresh every native PivotTable on one explicitly named worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: { sheet: { type: 'string' } },
        required: ['sheet'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          refreshed: { type: 'integer', minimum: 1, maximum: 100 },
        },
        required: ['sheet', 'refreshed'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.pivot.update',
      format: 'xlsx',
      family: 'pivot',
      summary: 'Replace the bounded layout of one explicitly identified native PivotTable.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          pivotId: { type: 'string' },
          targetCell: { type: 'string' },
          rowFields: {
            type: 'array',
            items: { type: 'string' },
            minItems: 1,
            maxItems: 8,
          },
          columnFields: {
            type: 'array',
            items: { type: 'string' },
            minItems: 0,
            maxItems: 8,
          },
          pageFields: {
            type: 'array',
            items: { type: 'string' },
            minItems: 0,
            maxItems: 4,
          },
          values: {
            type: 'array',
            items: {
              type: 'object',
              properties: {
                field: { type: 'string' },
                aggregation: {
                  type: 'string',
                  enum: ['sum', 'count', 'average', 'max', 'min'],
                },
                numberFormat: { type: ['string', 'null'] },
                showAs: {
                  type: ['string', 'null'],
                  enum: ['percentOfTotal', 'percentOfRow', 'percentOfCol', null],
                },
              },
              required: ['field', 'aggregation', 'numberFormat', 'showAs'],
              additionalProperties: false,
            },
            minItems: 1,
            maxItems: 8,
          },
        },
        required: ['pivotId', 'targetCell', 'rowFields', 'columnFields', 'pageFields', 'values'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          pivotId: { type: 'string' },
          targetCell: { type: 'string' },
        },
        required: ['pivotId', 'targetCell'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.pivot.set_member_filter',
      format: 'xlsx',
      family: 'pivot',
      summary: 'Set or clear the exact selected members of one native PivotTable field.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          pivotId: { type: 'string' },
          field: { type: 'string' },
          selectedValues: {
            type: ['array', 'null'],
            items: { type: ['string', 'number', 'boolean', 'null'] },
            minItems: 1,
            maxItems: 10_000,
          },
        },
        required: ['pivotId', 'field', 'selectedValues'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          pivotId: { type: 'string' },
          field: { type: 'string' },
          selectedCount: { type: ['integer', 'null'], minimum: 1, maximum: 10_000 },
        },
        required: ['pivotId', 'field', 'selectedCount'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.chart.update',
      format: 'xlsx',
      family: 'chart',
      summary: 'Apply a bounded final-state patch to one explicitly identified XLSX chart.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          chartId: { type: 'string' },
          title: { type: 'string' },
          type: {
            type: 'string',
            enum: ['column', 'bar', 'line', 'area', 'pie', 'doughnut'],
          },
          legend: { type: 'string', enum: ['none', 'right', 'bottom', 'top', 'left'] },
          dataLabels: {
            type: 'string',
            enum: ['none', 'value', 'percent', 'category-percent'],
          },
          grouping: {
            type: 'string',
            enum: ['clustered', 'stacked', 'percentStacked'],
          },
          categoryAxisTitle: { type: ['string', 'null'] },
          valueAxisTitle: { type: ['string', 'null'] },
          gridlines: { type: 'boolean' },
          valueAxisMin: { type: ['number', 'null'] },
          valueAxisMax: { type: ['number', 'null'] },
          gapWidthPct: { type: 'integer', minimum: 0, maximum: 500 },
          holeSizePct: { type: 'integer', minimum: 10, maximum: 90 },
          explosionPct: { type: 'integer', minimum: 0, maximum: 400 },
          dataLabelPosition: {
            type: 'string',
            enum: ['center', 'inside-end', 'outside-end'],
          },
          dataLabelFormat: { type: 'string' },
        },
        required: ['chartId'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          chartId: { type: 'string' },
        },
        required: ['changed', 'chartId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.chart.remove',
      format: 'xlsx',
      family: 'chart',
      summary: 'Remove one explicitly identified XLSX chart.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: { chartId: { type: 'string' } },
        required: ['chartId'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          chartId: { type: 'string' },
        },
        required: ['changed', 'chartId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.chart.set_colors',
      format: 'xlsx',
      family: 'chart',
      summary: 'Set explicit bounded series or point colors on an identified XLSX chart.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          chartId: { type: 'string' },
          seriesColors: { type: 'array', items: { type: 'string' } },
          pointColors: { type: 'array', items: { type: 'string' } },
        },
        required: ['chartId'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          chartId: { type: 'string' },
        },
        required: ['changed', 'chartId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.chart.set_series',
      format: 'xlsx',
      family: 'chart',
      summary: 'Replace an identified XLSX chart with an explicit bounded complete series set.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          chartId: { type: 'string' },
          series: {
            type: 'array',
            items: {
              type: 'object',
              properties: {
                name: { type: 'string' },
                values: { type: 'array', items: { type: 'number' } },
                categories: { type: 'array', items: { type: 'string' } },
                valuesRef: { type: 'string' },
                categoriesRef: { type: 'string' },
                color: { type: 'string' },
              },
              required: ['name', 'values'],
              additionalProperties: false,
            },
          },
        },
        required: ['chartId', 'series'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          chartId: { type: 'string' },
          seriesCount: { type: 'integer', minimum: 1, maximum: 24 },
        },
        required: ['changed', 'chartId', 'seriesCount'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.shape.add',
      format: 'xlsx',
      family: 'shape',
      summary: 'Add one bounded XLSX shape or text box at an explicit cell anchor.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          type: { type: 'string', enum: [...ADDABLE_SHAPE_TYPES, 'textbox'] },
          anchorCell: { type: 'string' },
          fillColor: { type: 'string' },
          text: { type: 'string' },
        },
        required: ['sheet', 'type', 'anchorCell'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          type: { type: 'string' },
          anchorCell: { type: 'string' },
          visualId: { type: 'string' },
        },
        required: ['sheet', 'type', 'anchorCell', 'visualId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.shape.update',
      format: 'xlsx',
      family: 'shape',
      summary: 'Apply a bounded final-state patch to one explicitly identified XLSX shape.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          shapeId: { type: 'string' },
          anchorCell: { type: 'string' },
          fillColor: { type: 'string' },
          text: { type: 'string' },
        },
        required: ['shapeId'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          shapeId: { type: 'string' },
        },
        required: ['changed', 'shapeId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.shape.remove',
      format: 'xlsx',
      family: 'shape',
      summary: 'Remove one explicitly identified XLSX shape.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: { shapeId: { type: 'string' } },
        required: ['shapeId'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          shapeId: { type: 'string' },
        },
        required: ['changed', 'shapeId'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.image.add',
      format: 'xlsx',
      family: 'image',
      summary: 'Add one local image at an explicit XLSX cell anchor.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          path: { type: 'string' },
          anchorCell: { type: 'string' },
        },
        required: ['sheet', 'path', 'anchorCell'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          anchorCell: { type: 'string' },
          visualId: { type: 'string' },
        },
        required: ['sheet', 'anchorCell', 'visualId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.image.add_staged',
      format: 'xlsx',
      family: 'image',
      summary: 'Add hydrated staged image bytes at an explicit XLSX cell anchor.',
      visibility: 'internal',
      inputSchema: {
        type: 'object',
        properties: {
          blobId: { type: 'string' },
          name: { type: 'string' },
          size: { type: 'integer', minimum: 1, maximum: 20_971_520 },
          data: {
            type: 'object',
            description: 'Hydrated ArrayBuffer supplied by the live-session host bridge.',
            properties: {},
            required: [],
            additionalProperties: false,
          },
          sheet: { type: 'string' },
          anchorCell: { type: 'string' },
        },
        required: ['blobId', 'name', 'size', 'data', 'sheet', 'anchorCell'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          anchorCell: { type: 'string' },
          visualId: { type: 'string' },
        },
        required: ['sheet', 'anchorCell', 'visualId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.image.move',
      format: 'xlsx',
      family: 'image',
      summary: 'Move one explicitly identified XLSX image to a final cell anchor.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          imageId: { type: 'string' },
          anchorCell: { type: 'string' },
        },
        required: ['imageId', 'anchorCell'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          imageId: { type: 'string' },
        },
        required: ['changed', 'imageId'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.image.remove',
      format: 'xlsx',
      family: 'image',
      summary: 'Remove one explicitly identified XLSX image.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: { imageId: { type: 'string' } },
        required: ['imageId'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          imageId: { type: 'string' },
        },
        required: ['changed', 'imageId'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.defined_name.set',
      format: 'xlsx',
      family: 'defined_name',
      summary: 'Create or update one workbook- or sheet-scoped XLSX defined name.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          name: { type: 'string' },
          formula: { type: 'string' },
          scopeSheet: { type: 'string' },
          previousName: { type: 'string' },
        },
        required: ['name', 'formula'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          name: { type: 'string' },
          formula: { type: 'string' },
          scope: { type: 'string' },
        },
        required: ['changed', 'name', 'formula', 'scope'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.defined_name.remove',
      format: 'xlsx',
      family: 'defined_name',
      summary: 'Remove one explicitly scoped XLSX defined name.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          name: { type: 'string' },
          scopeSheet: { type: 'string' },
        },
        required: ['name'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          name: { type: 'string' },
          scope: { type: 'string' },
        },
        required: ['changed', 'name', 'scope'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.note.set',
      format: 'xlsx',
      family: 'note',
      summary: 'Set the note text on one explicitly addressed XLSX cell.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          address: { type: 'string' },
          text: { type: 'string' },
        },
        required: ['sheet', 'address', 'text'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          sheet: { type: 'string' },
          address: { type: 'string' },
        },
        required: ['changed', 'sheet', 'address'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.note.remove',
      format: 'xlsx',
      family: 'note',
      summary: 'Remove the note from one explicitly addressed XLSX cell.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          address: { type: 'string' },
        },
        required: ['sheet', 'address'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', enum: [1] },
          sheet: { type: 'string' },
          address: { type: 'string' },
        },
        required: ['changed', 'sheet', 'address'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.column.delete',
      format: 'xlsx',
      family: 'column',
      summary: 'Delete columns from an XLSX column label.',
      visibility: 'agent',
      inputSchema: xlsxColumnMutationInputSchema,
      outputSchema: xlsxColumnMutationOutputSchema,
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.column.insert',
      format: 'xlsx',
      family: 'column',
      summary: 'Insert columns before an XLSX column label.',
      visibility: 'agent',
      inputSchema: xlsxColumnMutationInputSchema,
      outputSchema: xlsxColumnMutationOutputSchema,
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.column.set_width',
      format: 'xlsx',
      family: 'column',
      summary: 'Set an explicit XLSX column span to a final character width.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          column: { type: 'string' },
          count: xlsxStructuralCountSchema,
          widthCharacters: { type: 'number', minimum: 0, maximum: 255 },
        },
        required: ['sheet', 'column', 'count', 'widthCharacters'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          column: { type: 'string' },
          count: { type: 'integer', minimum: 1 },
          widthCharacters: { type: 'number', minimum: 0, maximum: 255 },
        },
        required: ['sheet', 'column', 'count', 'widthCharacters'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.column.set_visibility',
      format: 'xlsx',
      family: 'column',
      summary: 'Set the explicit visible state of a bounded XLSX column span.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          column: { type: 'string' },
          count: xlsxStructuralCountSchema,
          visible: { type: 'boolean' },
        },
        required: ['sheet', 'column', 'count', 'visible'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          column: { type: 'string' },
          count: { type: 'integer', minimum: 1 },
          visible: { type: 'boolean' },
        },
        required: ['sheet', 'column', 'count', 'visible'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.column.copy_widths',
      format: 'xlsx',
      family: 'column',
      summary: 'Copy explicit XLSX column widths between bounded column spans.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceColumn: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationColumn: { type: 'string' },
          count: xlsxStructuralCountSchema,
        },
        required: ['sourceSheet', 'sourceColumn', 'destinationSheet', 'destinationColumn', 'count'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceColumn: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationColumn: { type: 'string' },
          count: { type: 'integer', minimum: 1 },
        },
        required: ['sourceSheet', 'sourceColumn', 'destinationSheet', 'destinationColumn', 'count'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.formula.insert_aggregate',
      format: 'xlsx',
      family: 'formula',
      summary: 'Insert one aggregate formula below each column in an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          function: { type: 'string', enum: ['SUM', 'AVERAGE', 'COUNT', 'MAX', 'MIN'] },
        },
        required: ['sheet', 'range', 'function'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          targetRange: { type: 'string' },
          function: { type: 'string', enum: ['SUM', 'AVERAGE', 'COUNT', 'MAX', 'MIN'] },
          inserted: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'range', 'targetRange', 'function', 'inserted'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.history.undo',
      format: 'xlsx',
      family: 'history',
      summary: 'Undo the latest edit in the mounted XLSX workbook history.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {},
        required: [],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          undone: { type: 'boolean', enum: [true] },
        },
        required: ['undone'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.history.redo',
      format: 'xlsx',
      family: 'history',
      summary: 'Redo the latest undone edit in the mounted XLSX workbook history.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {},
        required: [],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          redone: { type: 'boolean', enum: [true] },
        },
        required: ['redone'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.flash_fill',
      format: 'xlsx',
      family: 'range',
      summary: 'Infer a pattern from examples and fill empty cells in an XLSX target column.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
        },
        required: ['sheet', 'range'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          targetRange: { type: 'string' },
          filled: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'range', 'targetRange', 'filled'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.move',
      format: 'xlsx',
      family: 'range',
      summary: 'Move one explicit bounded XLSX range to an equally shaped destination range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
          moved: { type: 'integer', minimum: 1, maximum: 20_000 },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange', 'moved'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.copy_values',
      format: 'xlsx',
      family: 'range',
      summary: 'Copy computed scalar values between explicit XLSX ranges.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
          copied: { type: 'integer', minimum: 1 },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange', 'copied'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.copy_formulas',
      format: 'xlsx',
      family: 'range',
      summary: 'Copy formulas and scalar cells between explicit XLSX ranges.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
          copied: { type: 'integer', minimum: 1 },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange', 'copied'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.copy_formats',
      format: 'xlsx',
      family: 'range',
      summary: 'Copy cell formats between explicit XLSX ranges without changing contents.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
          copied: { type: 'integer', minimum: 1 },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange', 'copied'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.copy_without_borders',
      format: 'xlsx',
      family: 'range',
      summary: 'Copy cells and non-border formats while preserving destination borders.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          destinationSheet: { type: 'string' },
          destinationRange: { type: 'string' },
          copied: { type: 'integer', minimum: 1 },
        },
        required: ['sourceSheet', 'sourceRange', 'destinationSheet', 'destinationRange', 'copied'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.text_to_columns',
      format: 'xlsx',
      family: 'range',
      summary: 'Split one XLSX text column using an explicit retained delimiter.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          delimiter: { type: 'string', enum: ['tab', 'comma', 'semicolon', 'space'] },
        },
        required: ['sheet', 'range', 'delimiter'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          delimiter: { type: 'string', enum: ['tab', 'comma', 'semicolon', 'space'] },
        },
        required: ['sheet', 'range', 'delimiter'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_values',
      format: 'xlsx',
      family: 'range',
      summary: 'Set a bounded scalar matrix in an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          values: {
            type: 'array',
            minItems: 1,
            items: {
              type: 'array',
              minItems: 1,
              items: { type: ['string', 'number', 'boolean', 'null'] },
            },
          },
        },
        required: ['sheet', 'range', 'values'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          changed: { type: 'integer', minimum: 1 },
          sheet: { type: 'string' },
          range: { type: 'string' },
        },
        required: ['changed', 'sheet', 'range'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.replace_text',
      format: 'xlsx',
      family: 'range',
      summary: 'Replace literal text values inside one explicit bounded XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          find: { type: 'string' },
          replace: { type: 'string' },
          matchCase: { type: 'boolean' },
          wholeCell: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'find', 'replace', 'matchCase', 'wholeCell'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          changed: { type: 'integer', minimum: 0, maximum: 20_000 },
        },
        required: ['sheet', 'range', 'changed'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.insert_subtotals',
      format: 'xlsx',
      family: 'range',
      summary: 'Insert grouped subtotal rows and one grand-total row for an explicit XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          groupColumn: { type: 'string' },
          valueColumn: { type: 'string' },
          aggregation: { type: 'string', enum: ['sum', 'count', 'average'] },
        },
        required: ['sheet', 'range', 'groupColumn', 'valueColumn', 'aggregation'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          groupColumn: { type: 'string' },
          valueColumn: { type: 'string' },
          aggregation: { type: 'string', enum: ['sum', 'count', 'average'] },
          groups: { type: 'integer', minimum: 1, maximum: 200 },
          insertedRows: { type: 'integer', minimum: 2, maximum: 201 },
        },
        required: [
          'sheet',
          'range',
          'groupColumn',
          'valueColumn',
          'aggregation',
          'groups',
          'insertedRows',
        ],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.consolidate',
      format: 'xlsx',
      family: 'range',
      summary: 'Consolidate explicit XLSX source ranges into formulas at one target cell.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          targetSheet: { type: 'string' },
          targetCell: { type: 'string' },
          sources: {
            type: 'array',
            minItems: 1,
            maxItems: 20,
            items: {
              type: 'object',
              properties: {
                sheet: { type: 'string' },
                range: { type: 'string' },
              },
              required: ['sheet', 'range'],
              additionalProperties: false,
            },
          },
          aggregation: { type: 'string', enum: ['sum', 'count', 'average', 'max', 'min'] },
          leftLabels: { type: 'boolean' },
        },
        required: ['targetSheet', 'targetCell', 'sources', 'aggregation', 'leftLabels'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          targetSheet: { type: 'string' },
          targetCell: { type: 'string' },
          sources: { type: 'integer', minimum: 1, maximum: 20 },
          aggregation: { type: 'string', enum: ['sum', 'count', 'average', 'max', 'min'] },
          leftLabels: { type: 'boolean' },
          rows: { type: 'integer', minimum: 1, maximum: 50_000 },
          columns: { type: 'integer', minimum: 1, maximum: 16_384 },
        },
        required: [
          'targetSheet',
          'targetCell',
          'sources',
          'aggregation',
          'leftLabels',
          'rows',
          'columns',
        ],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_filter',
      format: 'xlsx',
      family: 'range',
      summary: 'Set the explicit auto-filter state of an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'enabled'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'enabled'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_checkbox',
      format: 'xlsx',
      family: 'range',
      summary: 'Set an XLSX range to a final checkbox-validation state.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'enabled'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'enabled'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_list_validation',
      format: 'xlsx',
      family: 'range',
      summary: 'Set explicit bounded list validation on an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          values: {
            type: 'array',
            items: { type: 'string' },
            minItems: 1,
            maxItems: 100,
          },
          allowBlank: { type: 'boolean' },
          showDropdown: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'values', 'allowBlank', 'showDropdown'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          valueCount: { type: 'integer', minimum: 1 },
          allowBlank: { type: 'boolean' },
          showDropdown: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'valueCount', 'allowBlank', 'showDropdown'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.remove_data_validation',
      format: 'xlsx',
      family: 'range',
      summary: 'Remove data validation from an explicit XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
        },
        required: ['sheet', 'range'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          removed: { type: 'boolean', enum: [true] },
        },
        required: ['sheet', 'range', 'removed'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_list_reference_validation',
      format: 'xlsx',
      family: 'range',
      summary: 'Set range-backed list validation on an explicit XLSX target.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          allowBlank: { type: 'boolean' },
          showDropdown: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'sourceSheet', 'sourceRange', 'allowBlank', 'showDropdown'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          sourceSheet: { type: 'string' },
          sourceRange: { type: 'string' },
          allowBlank: { type: 'boolean' },
          showDropdown: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'sourceSheet', 'sourceRange', 'allowBlank', 'showDropdown'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_comparison_validation',
      format: 'xlsx',
      family: 'range',
      summary: 'Set one bounded scalar comparison validation rule on an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          kind: {
            type: 'string',
            enum: ['whole', 'decimal', 'date', 'time', 'textLength'],
          },
          operator: {
            type: 'string',
            enum: [
              'between',
              'notBetween',
              'equal',
              'notEqual',
              'greaterThan',
              'greaterThanOrEqual',
              'lessThan',
              'lessThanOrEqual',
            ],
          },
          operand1: { type: ['string', 'number'] },
          operand2: { type: ['string', 'number'] },
          allowBlank: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'kind', 'operator', 'operand1', 'allowBlank'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          kind: { type: 'string' },
          operator: { type: 'string' },
          operand1: { type: ['string', 'number'] },
          operand2: { type: ['string', 'number'] },
          allowBlank: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'kind', 'operator', 'operand1', 'allowBlank'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_custom_formula_validation',
      format: 'xlsx',
      family: 'range',
      summary: 'Set explicit custom-formula validation on an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          formula: { type: 'string' },
          allowBlank: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'formula', 'allowBlank'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          formula: { type: 'string' },
          allowBlank: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'formula', 'allowBlank'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_validation_messages',
      format: 'xlsx',
      family: 'range',
      summary: 'Set explicit input and error messages on an existing XLSX validation rule.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          inputTitle: { type: ['string', 'null'] },
          inputMessage: { type: ['string', 'null'] },
          errorStyle: {
            type: 'string',
            enum: ['none', 'stop', 'warning', 'information'],
          },
          errorTitle: { type: ['string', 'null'] },
          errorMessage: { type: ['string', 'null'] },
        },
        required: [
          'sheet',
          'range',
          'inputTitle',
          'inputMessage',
          'errorStyle',
          'errorTitle',
          'errorMessage',
        ],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          inputTitle: { type: ['string', 'null'] },
          inputMessage: { type: ['string', 'null'] },
          errorStyle: { type: 'string' },
          errorTitle: { type: ['string', 'null'] },
          errorMessage: { type: ['string', 'null'] },
        },
        required: [
          'sheet',
          'range',
          'inputTitle',
          'inputMessage',
          'errorStyle',
          'errorTitle',
          'errorMessage',
        ],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.conditional_format.set_comparison',
      format: 'xlsx',
      family: 'conditional_format',
      summary:
        'Create or replace one explicitly identified bounded XLSX comparison conditional-format rule.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: ['string', 'null'] },
          operator: {
            type: 'string',
            enum: [
              'between',
              'notBetween',
              'equal',
              'notEqual',
              'greaterThan',
              'greaterThanOrEqual',
              'lessThan',
              'lessThanOrEqual',
            ],
          },
          operand1: { type: 'number' },
          operand2: { type: 'number' },
          format: {
            type: 'object',
            properties: {
              fillColor: { type: 'string' },
              fontColor: { type: 'string' },
              bold: { type: 'boolean' },
              italic: { type: 'boolean' },
            },
            additionalProperties: false,
          },
          stopIfTrue: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'ruleId', 'operator', 'operand1', 'format', 'stopIfTrue'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: 'string' },
          created: { type: 'boolean' },
          operator: { type: 'string' },
          operand1: { type: 'number' },
          operand2: { type: 'number' },
          stopIfTrue: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'ruleId', 'created', 'operator', 'operand1', 'stopIfTrue'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.conditional_format.remove',
      format: 'xlsx',
      family: 'conditional_format',
      summary: 'Remove one explicitly identified XLSX conditional-format rule.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          ruleId: { type: 'string' },
        },
        required: ['sheet', 'ruleId'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          ruleId: { type: 'string' },
          removed: { type: 'boolean', enum: [true] },
        },
        required: ['sheet', 'ruleId', 'removed'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.conditional_format.clear',
      format: 'xlsx',
      family: 'conditional_format',
      summary: 'Clear XLSX conditional-format rules from one explicit range or worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          scope: { type: 'string', enum: ['range', 'sheet'] },
          range: { type: ['string', 'null'] },
        },
        required: ['sheet', 'scope', 'range'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          scope: { type: 'string', enum: ['range', 'sheet'] },
          range: { type: ['string', 'null'] },
          cleared: { type: 'integer', minimum: 0 },
        },
        required: ['sheet', 'scope', 'range', 'cleared'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.conditional_format.set_priority',
      format: 'xlsx',
      family: 'conditional_format',
      summary: 'Set one XLSX conditional-format rule to an explicit one-based priority position.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          ruleId: { type: 'string' },
          position: { type: 'integer', minimum: 1, maximum: 100 },
        },
        required: ['sheet', 'ruleId', 'position'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          ruleId: { type: 'string' },
          position: { type: 'integer', minimum: 1, maximum: 100 },
          moved: { type: 'boolean' },
        },
        required: ['sheet', 'ruleId', 'position', 'moved'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.conditional_format.set_highlight',
      format: 'xlsx',
      family: 'conditional_format',
      summary:
        'Create or replace one bounded XLSX text, blank-state, or duplicate-state highlight rule.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: ['string', 'null'] },
          predicate: {
            type: 'string',
            enum: [
              'textContains',
              'textNotContains',
              'textStartsWith',
              'textEndsWith',
              'textEqual',
              'textNotEqual',
              'blank',
              'nonBlank',
              'error',
              'nonError',
              'duplicate',
              'unique',
            ],
          },
          text: { type: ['string', 'null'] },
          format: {
            type: 'object',
            properties: {
              fillColor: { type: 'string' },
              fontColor: { type: 'string' },
              bold: { type: 'boolean' },
              italic: { type: 'boolean' },
            },
            additionalProperties: false,
          },
          stopIfTrue: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'ruleId', 'predicate', 'text', 'format', 'stopIfTrue'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: 'string' },
          created: { type: 'boolean' },
          predicate: { type: 'string' },
          text: { type: ['string', 'null'] },
          stopIfTrue: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'ruleId', 'created', 'predicate', 'text', 'stopIfTrue'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.conditional_format.set_statistical',
      format: 'xlsx',
      family: 'conditional_format',
      summary: 'Create or replace one bounded XLSX rank or average conditional-format rule.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: ['string', 'null'] },
          kind: { type: 'string', enum: ['rank', 'average'] },
          direction: { type: 'string', enum: ['top', 'bottom', 'above', 'below'] },
          rank: { type: ['integer', 'null'] },
          percent: { type: ['boolean', 'null'] },
          inclusive: { type: ['boolean', 'null'] },
          format: {
            type: 'object',
            properties: {
              fillColor: { type: 'string' },
              fontColor: { type: 'string' },
              bold: { type: 'boolean' },
              italic: { type: 'boolean' },
            },
            additionalProperties: false,
          },
          stopIfTrue: { type: 'boolean' },
        },
        required: [
          'sheet',
          'range',
          'ruleId',
          'kind',
          'direction',
          'rank',
          'percent',
          'inclusive',
          'format',
          'stopIfTrue',
        ],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: 'string' },
          created: { type: 'boolean' },
          kind: { type: 'string' },
          direction: { type: 'string' },
          rank: { type: ['integer', 'null'] },
          percent: { type: ['boolean', 'null'] },
          inclusive: { type: ['boolean', 'null'] },
          stopIfTrue: { type: 'boolean' },
        },
        required: [
          'sheet',
          'range',
          'ruleId',
          'created',
          'kind',
          'direction',
          'rank',
          'percent',
          'inclusive',
          'stopIfTrue',
        ],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.conditional_format.set_formula',
      format: 'xlsx',
      family: 'conditional_format',
      summary: 'Create or replace one bounded XLSX custom-formula conditional-format rule.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: ['string', 'null'] },
          formula: { type: 'string' },
          format: {
            type: 'object',
            properties: {
              fillColor: { type: 'string' },
              fontColor: { type: 'string' },
              bold: { type: 'boolean' },
              italic: { type: 'boolean' },
            },
            additionalProperties: false,
          },
          stopIfTrue: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'ruleId', 'formula', 'format', 'stopIfTrue'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: 'string' },
          created: { type: 'boolean' },
          formula: { type: 'string' },
          stopIfTrue: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'ruleId', 'created', 'formula', 'stopIfTrue'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.conditional_format.set_visual',
      format: 'xlsx',
      family: 'conditional_format',
      summary:
        'Create or replace one bounded saveable XLSX color-scale, data-bar, or icon-set rule.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: ['string', 'null'] },
          kind: { type: 'string', enum: ['colorScale', 'dataBar', 'iconSet'] },
          colors: {
            type: 'array',
            items: { type: 'string' },
            minItems: 0,
            maxItems: 3,
          },
          thresholds: {
            type: 'array',
            items: {
              type: 'object',
              properties: {
                type: {
                  type: 'string',
                  enum: ['min', 'max', 'num', 'percent', 'percentile', 'formula'],
                },
                value: { type: ['number', 'string', 'null'] },
                inclusive: { type: ['boolean', 'null'] },
              },
              required: ['type', 'value', 'inclusive'],
              additionalProperties: false,
            },
            minItems: 1,
            maxItems: 5,
          },
          iconSet: { type: ['string', 'null'] },
          showValue: { type: ['boolean', 'null'] },
          reverse: { type: ['boolean', 'null'] },
          gradient: { type: ['boolean', 'null'] },
          stopIfTrue: { type: 'boolean' },
        },
        required: [
          'sheet',
          'range',
          'ruleId',
          'kind',
          'colors',
          'thresholds',
          'iconSet',
          'showValue',
          'reverse',
          'gradient',
          'stopIfTrue',
        ],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          ruleId: { type: 'string' },
          created: { type: 'boolean' },
          kind: { type: 'string' },
          stopIfTrue: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'ruleId', 'created', 'kind', 'stopIfTrue'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.clear_filter_criteria',
      format: 'xlsx',
      family: 'range',
      summary: 'Clear every criterion while retaining an XLSX auto-filter range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
        },
        required: ['sheet', 'range'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          cleared: { type: 'boolean', enum: [true] },
        },
        required: ['sheet', 'range', 'cleared'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_filter_values',
      format: 'xlsx',
      family: 'range',
      summary: 'Set a bounded value-list criterion on one XLSX auto-filter column.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          column: { type: 'string' },
          values: {
            type: 'array',
            minItems: 1,
            maxItems: 10_000,
            items: { type: 'string' },
          },
          includeBlank: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'column', 'values', 'includeBlank'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          column: { type: 'string' },
          selectedValues: { type: 'integer', minimum: 1, maximum: 10_000 },
          includeBlank: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'column', 'selectedValues', 'includeBlank'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_custom_filter',
      format: 'xlsx',
      family: 'range',
      summary: 'Set bounded custom conditions on one XLSX auto-filter column.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          column: { type: 'string' },
          conjunction: { type: 'string', enum: ['and', 'or'] },
          conditions: {
            type: 'array',
            minItems: 1,
            maxItems: 2,
            items: {
              type: 'object',
              properties: {
                operator: {
                  type: 'string',
                  enum: [
                    'equal',
                    'notEqual',
                    'greaterThan',
                    'greaterThanOrEqual',
                    'lessThan',
                    'lessThanOrEqual',
                  ],
                },
                value: { type: 'string' },
              },
              required: ['operator', 'value'],
              additionalProperties: false,
            },
          },
        },
        required: ['sheet', 'range', 'column', 'conjunction', 'conditions'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          column: { type: 'string' },
          conjunction: { type: 'string', enum: ['and', 'or'] },
          conditions: { type: 'integer', minimum: 1, maximum: 2 },
        },
        required: ['sheet', 'range', 'column', 'conjunction', 'conditions'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_alignment',
      format: 'xlsx',
      family: 'range',
      summary: 'Set explicit alignment fields on an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          alignment: {
            type: 'object',
            properties: {
              horizontal: {
                type: ['string', 'null'],
                enum: ['left', 'center', 'right', 'justify', 'distributed', null],
              },
              vertical: {
                type: ['string', 'null'],
                enum: ['top', 'middle', 'bottom', null],
              },
              wrap: { type: 'boolean' },
              indent: { type: 'integer', minimum: 0, maximum: 250 },
              rotation: {
                type: 'object',
                properties: {
                  mode: { type: 'string', enum: ['angle', 'none', 'stacked'] },
                  degrees: { type: 'integer', minimum: -90, maximum: 90 },
                },
                required: ['mode'],
                additionalProperties: false,
              },
            },
            required: [],
            additionalProperties: false,
          },
          fields: {
            type: 'array',
            minItems: 1,
            items: {
              type: 'string',
              enum: ['horizontal', 'vertical', 'wrap', 'indent', 'rotation'],
            },
          },
        },
        required: ['sheet', 'range', 'alignment', 'fields'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          fields: {
            type: 'array',
            minItems: 1,
            items: {
              type: 'string',
              enum: ['horizontal', 'vertical', 'wrap', 'indent', 'rotation'],
            },
          },
        },
        required: ['sheet', 'range', 'fields'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_text_style',
      format: 'xlsx',
      family: 'range',
      summary: 'Set explicit text style fields on an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          style: {
            type: 'object',
            properties: {
              bold: { type: 'boolean' },
              italic: { type: 'boolean' },
              strike: { type: 'boolean' },
              underline: { type: 'string', enum: ['none', 'single', 'double'] },
            },
            required: [],
            additionalProperties: false,
          },
          fields: {
            type: 'array',
            minItems: 1,
            items: { type: 'string', enum: ['bold', 'italic', 'strike', 'underline'] },
          },
        },
        required: ['sheet', 'range', 'style', 'fields'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          fields: {
            type: 'array',
            minItems: 1,
            items: { type: 'string', enum: ['bold', 'italic', 'strike', 'underline'] },
          },
        },
        required: ['sheet', 'range', 'fields'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_font',
      format: 'xlsx',
      family: 'range',
      summary: 'Set explicit font appearance fields on an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          font: {
            type: 'object',
            properties: {
              family: { type: ['string', 'null'] },
              size: { type: ['number', 'null'], minimum: 1, maximum: 409 },
              color: { type: ['string', 'null'] },
            },
            required: [],
            additionalProperties: false,
          },
          fields: {
            type: 'array',
            minItems: 1,
            items: { type: 'string', enum: ['family', 'size', 'color'] },
          },
        },
        required: ['sheet', 'range', 'font', 'fields'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          fields: {
            type: 'array',
            minItems: 1,
            items: { type: 'string', enum: ['family', 'size', 'color'] },
          },
        },
        required: ['sheet', 'range', 'fields'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_fill',
      format: 'xlsx',
      family: 'range',
      summary: 'Set or clear the fill color on an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          color: { type: ['string', 'null'] },
        },
        required: ['sheet', 'range', 'color'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
        },
        required: ['sheet', 'range'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_border',
      format: 'xlsx',
      family: 'range',
      summary: 'Set or clear an explicit border preset on an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          border: {
            type: 'object',
            properties: {
              preset: {
                type: 'string',
                enum: ['none', 'all', 'outer', 'thick-outer', 'top', 'bottom', 'left', 'right'],
              },
              lineStyle: {
                type: 'string',
                enum: ['thin', 'medium', 'thick', 'double', 'hair', 'dashed', 'dotted'],
              },
              color: { type: 'string' },
            },
            required: ['preset'],
            additionalProperties: false,
          },
        },
        required: ['sheet', 'range', 'border'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          preset: {
            type: 'string',
            enum: ['none', 'all', 'outer', 'thick-outer', 'top', 'bottom', 'left', 'right'],
          },
        },
        required: ['sheet', 'range', 'preset'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.apply_cell_style',
      format: 'xlsx',
      family: 'range',
      summary: 'Apply a named built-in cell style to an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          preset: {
            type: 'string',
            enum: [
              'good',
              'bad',
              'neutral',
              'input',
              'output',
              'calculation',
              'warning-text',
              'title',
              'heading-1',
              'heading-2',
              'total',
              'accent1-20',
              'accent1-40',
              'accent1',
            ],
          },
        },
        required: ['sheet', 'range', 'preset'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          preset: { type: 'string' },
        },
        required: ['sheet', 'range', 'preset'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_number_format',
      format: 'xlsx',
      family: 'range',
      summary: 'Assign an explicit number-format pattern to an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          pattern: { type: 'string' },
        },
        required: ['sheet', 'range', 'pattern'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          pattern: { type: 'string' },
        },
        required: ['sheet', 'range', 'pattern'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.merge',
      format: 'xlsx',
      family: 'range',
      summary: 'Merge, merge across, merge and center, or unmerge an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          mode: { type: 'string', enum: ['cells', 'across', 'center', 'unmerge'] },
        },
        required: ['sheet', 'range', 'mode'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          mode: { type: 'string', enum: ['cells', 'across', 'center', 'unmerge'] },
        },
        required: ['sheet', 'range', 'mode'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.clear',
      format: 'xlsx',
      family: 'range',
      summary: 'Clear contents, formats, or all data and formatting from an XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          scope: { type: 'string', enum: ['contents', 'formats', 'all'] },
        },
        required: ['sheet', 'range', 'scope'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          scope: { type: 'string', enum: ['contents', 'formats', 'all'] },
        },
        required: ['sheet', 'range', 'scope'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.fill',
      format: 'xlsx',
      family: 'range',
      summary: 'Fill an XLSX range down or right from its leading cells.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          direction: { type: 'string', enum: ['down', 'right'] },
        },
        required: ['sheet', 'range', 'direction'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          direction: { type: 'string', enum: ['down', 'right'] },
        },
        required: ['sheet', 'range', 'direction'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.sort',
      format: 'xlsx',
      family: 'range',
      summary: 'Sort an XLSX range by its first column.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          direction: { type: 'string', enum: ['asc', 'desc'] },
        },
        required: ['sheet', 'range', 'direction'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          direction: { type: 'string', enum: ['asc', 'desc'] },
        },
        required: ['sheet', 'range', 'direction'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.sort_custom',
      format: 'xlsx',
      family: 'range',
      summary: 'Sort an XLSX range by ordered column keys with optional headers.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          keys: {
            type: 'array',
            minItems: 1,
            items: {
              type: 'object',
              properties: {
                column: { type: 'string' },
                direction: { type: 'string', enum: ['asc', 'desc'] },
              },
              required: ['column', 'direction'],
              additionalProperties: false,
            },
          },
          hasHeader: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'keys', 'hasHeader'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          keys: {
            type: 'array',
            minItems: 1,
            items: {
              type: 'object',
              properties: {
                column: { type: 'string' },
                direction: { type: 'string', enum: ['asc', 'desc'] },
              },
              required: ['column', 'direction'],
              additionalProperties: false,
            },
          },
          hasHeader: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'keys', 'hasHeader'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.remove_duplicates',
      format: 'xlsx',
      family: 'range',
      summary: 'Remove duplicate rows from an XLSX range while keeping the first occurrence.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          hasHeader: { type: 'boolean' },
        },
        required: ['sheet', 'range', 'hasHeader'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          removed: { type: 'integer', minimum: 0 },
        },
        required: ['sheet', 'range', 'removed'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document', 'selection'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.range.set_protection',
      format: 'xlsx',
      family: 'range',
      summary: 'Set explicit OOXML protection fields on a bounded XLSX range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          protection: {
            type: 'object',
            properties: {
              locked: { type: 'boolean' },
              hidden: { type: 'boolean' },
            },
            required: [],
            additionalProperties: false,
          },
          fields: {
            type: 'array',
            minItems: 1,
            items: { type: 'string', enum: ['locked', 'hidden'] },
          },
        },
        required: ['sheet', 'range', 'protection', 'fields'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          fields: {
            type: 'array',
            minItems: 1,
            items: { type: 'string', enum: ['locked', 'hidden'] },
          },
        },
        required: ['sheet', 'range', 'fields'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: false,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.hyperlink.set',
      format: 'xlsx',
      family: 'hyperlink',
      summary: 'Set a normalized hyperlink target on one XLSX cell.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          address: { type: 'string' },
          target: { type: 'string' },
        },
        required: ['sheet', 'address', 'target'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          address: { type: 'string' },
          target: { type: 'string' },
        },
        required: ['sheet', 'address', 'target'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: false,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.hyperlink.remove',
      format: 'xlsx',
      family: 'hyperlink',
      summary: 'Remove a hyperlink and its link appearance from one XLSX cell.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          address: { type: 'string' },
        },
        required: ['sheet', 'address'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          address: { type: 'string' },
          removed: { type: 'boolean' },
        },
        required: ['sheet', 'address', 'removed'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: false,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.table.add',
      format: 'xlsx',
      family: 'table',
      summary: 'Create a styled XLSX table over an explicit range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          style: { type: 'string', enum: xlsxTableStyles },
        },
        required: ['sheet', 'range', 'style'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: 'string' },
          name: { type: 'string' },
          style: { type: 'string', enum: xlsxTableStyles },
        },
        required: ['sheet', 'range', 'name', 'style'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: false,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.table.insert_rows',
      format: 'xlsx',
      family: 'table',
      summary: 'Insert blank data rows into a session-created XLSX table.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1, maximum: 1_000 },
        },
        required: ['sheet', 'table', 'row', 'count'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'table', 'row', 'count'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.table.delete_rows',
      format: 'xlsx',
      family: 'table',
      summary: 'Delete bounded data rows from a session-created XLSX table.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1, maximum: 1_000 },
        },
        required: ['sheet', 'table', 'row', 'count'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'table', 'row', 'count'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.table.insert_columns',
      format: 'xlsx',
      family: 'table',
      summary: 'Insert named columns into a session-created XLSX table.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          column: { type: 'integer', minimum: 1 },
          columnName: { type: 'string' },
          count: { type: 'integer', minimum: 1, maximum: 100 },
        },
        required: ['sheet', 'table', 'column', 'columnName', 'count'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          column: { type: 'integer', minimum: 1 },
          columnName: { type: 'string' },
          count: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'table', 'column', 'columnName', 'count'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.table.delete_columns',
      format: 'xlsx',
      family: 'table',
      summary: 'Delete bounded columns from a session-created XLSX table.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          column: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1, maximum: 100 },
        },
        required: ['sheet', 'table', 'column', 'count'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          column: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'table', 'column', 'count'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.table.convert_to_range',
      format: 'xlsx',
      family: 'table',
      summary: 'Convert a session-created XLSX table back to its ordinary cell range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
        },
        required: ['sheet', 'table'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          table: { type: 'string' },
          converted: { type: 'boolean', enum: [true] },
        },
        required: ['sheet', 'table', 'converted'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sparkline.add',
      format: 'xlsx',
      family: 'sparkline',
      summary: 'Add row-aligned XLSX sparklines to an explicit target range.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          sourceRange: { type: 'string' },
          targetRange: { type: 'string' },
          type: { type: 'string', enum: ['line', 'column', 'stacked'] },
        },
        required: ['sheet', 'sourceRange', 'targetRange', 'type'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          sourceRange: { type: 'string' },
          targetRange: { type: 'string' },
          type: { type: 'string', enum: ['line', 'column', 'stacked'] },
          count: { type: 'integer', minimum: 1, maximum: 200 },
        },
        required: ['sheet', 'sourceRange', 'targetRange', 'type', 'count'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.outline.set_level',
      format: 'xlsx',
      family: 'outline',
      summary: 'Set a bounded XLSX row or column span to an explicit final outline level.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          axis: { type: 'string', enum: ['rows', 'columns'] },
          start: { type: 'integer', minimum: 1, maximum: 1_048_576 },
          count: xlsxStructuralCountSchema,
          level: { type: 'integer', minimum: 0, maximum: 7 },
        },
        required: ['sheet', 'axis', 'start', 'count', 'level'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          axis: { type: 'string', enum: ['rows', 'columns'] },
          start: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1 },
          level: { type: 'integer', minimum: 0, maximum: 7 },
        },
        required: ['sheet', 'axis', 'start', 'count', 'level'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.outline.set_detail_visibility',
      format: 'xlsx',
      family: 'outline',
      summary:
        'Set bounded XLSX outline details to a final visibility state and update their summary.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          axis: { type: 'string', enum: ['rows', 'columns'] },
          start: { type: 'integer', minimum: 1, maximum: 1_048_575 },
          count: xlsxStructuralCountSchema,
          hidden: { type: 'boolean' },
        },
        required: ['sheet', 'axis', 'start', 'count', 'hidden'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          axis: { type: 'string', enum: ['rows', 'columns'] },
          start: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1 },
          hidden: { type: 'boolean' },
        },
        required: ['sheet', 'axis', 'start', 'count', 'hidden'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.row.set_height',
      format: 'xlsx',
      family: 'row',
      summary: 'Set an explicit XLSX row span to a final height in points.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: xlsxStructuralCountSchema,
          heightPoints: { type: 'number', minimum: 0, maximum: 409.5 },
        },
        required: ['sheet', 'row', 'count', 'heightPoints'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1 },
          heightPoints: { type: 'number', minimum: 0, maximum: 409.5 },
        },
        required: ['sheet', 'row', 'count', 'heightPoints'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.row.set_visibility',
      format: 'xlsx',
      family: 'row',
      summary: 'Set the explicit visible state of a bounded XLSX row span.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: xlsxStructuralCountSchema,
          visible: { type: 'boolean' },
        },
        required: ['sheet', 'row', 'count', 'visible'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1 },
          visible: { type: 'boolean' },
        },
        required: ['sheet', 'row', 'count', 'visible'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.row.move',
      format: 'xlsx',
      family: 'row',
      summary: 'Move one explicit bounded XLSX row span before another row.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          row: { type: 'integer', minimum: 1, maximum: 1_048_576 },
          count: xlsxStructuralCountSchema,
          beforeRow: { type: 'integer', minimum: 1, maximum: 1_048_576 },
        },
        required: ['sheet', 'row', 'count', 'beforeRow'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          row: { type: 'integer', minimum: 1 },
          count: { type: 'integer', minimum: 1 },
          beforeRow: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'row', 'count', 'beforeRow'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.row.insert',
      format: 'xlsx',
      family: 'row',
      summary: 'Insert rows before a 1-based XLSX row position.',
      visibility: 'agent',
      inputSchema: xlsxRowMutationInputSchema,
      outputSchema: xlsxRowMutationOutputSchema,
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_fit_to_pages',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the explicit fit-to-page width and height of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          widthPages: { type: 'integer', minimum: 0, maximum: 1_000 },
          heightPages: { type: 'integer', minimum: 0, maximum: 1_000 },
        },
        required: ['sheet', 'widthPages', 'heightPages'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          widthPages: { type: 'integer', minimum: 0, maximum: 1_000 },
          heightPages: { type: 'integer', minimum: 0, maximum: 1_000 },
        },
        required: ['sheet', 'widthPages', 'heightPages'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_formula_view',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the explicit formula-view state of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'enabled'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'enabled'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'view'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_freeze',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the explicit frozen row and column counts of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          frozenRows: { type: 'integer', minimum: 0, maximum: 1_048_575 },
          frozenColumns: { type: 'integer', minimum: 0, maximum: 16_383 },
        },
        required: ['sheet', 'frozenRows', 'frozenColumns'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          frozenRows: { type: 'integer', minimum: 0, maximum: 1_048_575 },
          frozenColumns: { type: 'integer', minimum: 0, maximum: 16_383 },
        },
        required: ['sheet', 'frozenRows', 'frozenColumns'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'view'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_gridlines',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the explicit gridline visibility of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          visible: { type: 'boolean' },
        },
        required: ['sheet', 'visible'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          visible: { type: 'boolean' },
        },
        required: ['sheet', 'visible'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'view'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_header_footer',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set or clear the printed header and footer sections of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          header: {
            type: ['object', 'null'],
            properties: {
              left: { type: 'string' },
              center: { type: 'string' },
              right: { type: 'string' },
            },
            required: [],
            additionalProperties: false,
          },
          footer: {
            type: ['object', 'null'],
            properties: {
              left: { type: 'string' },
              center: { type: 'string' },
              right: { type: 'string' },
            },
            required: [],
            additionalProperties: false,
          },
        },
        required: ['sheet', 'header', 'footer'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          header: {
            type: ['object', 'null'],
            properties: {
              left: { type: 'string' },
              center: { type: 'string' },
              right: { type: 'string' },
            },
            required: [],
            additionalProperties: false,
          },
          footer: {
            type: ['object', 'null'],
            properties: {
              left: { type: 'string' },
              center: { type: 'string' },
              right: { type: 'string' },
            },
            required: [],
            additionalProperties: false,
          },
        },
        required: ['sheet', 'header', 'footer'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_page_margins',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the explicit print margins preset of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          margins: { type: 'string', enum: ['normal', 'wide', 'narrow'] },
        },
        required: ['sheet', 'margins'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          margins: { type: 'string', enum: ['normal', 'wide', 'narrow'] },
        },
        required: ['sheet', 'margins'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_page_orientation',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the explicit print orientation of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          orientation: { type: 'string', enum: ['portrait', 'landscape'] },
        },
        required: ['sheet', 'orientation'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          orientation: { type: 'string', enum: ['portrait', 'landscape'] },
        },
        required: ['sheet', 'orientation'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_paper_size',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the explicit paper-size preset of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          paperSize: { type: 'integer', enum: [1, 3, 5, 7, 8, 9, 11] },
        },
        required: ['sheet', 'paperSize'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          paperSize: { type: 'integer', enum: [1, 3, 5, 7, 8, 9, 11] },
        },
        required: ['sheet', 'paperSize'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_print_gridlines',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set whether gridlines print for an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'enabled'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'enabled'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_print_headings',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set whether row and column headings print for an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'enabled'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          enabled: { type: 'boolean' },
        },
        required: ['sheet', 'enabled'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_print_area',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set or clear the print area of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: ['string', 'null'] },
        },
        required: ['sheet', 'range'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          range: { type: ['string', 'null'] },
        },
        required: ['sheet', 'range'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_print_titles',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set or clear the print-title rows of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          rows: { type: ['string', 'null'] },
        },
        required: ['sheet', 'rows'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          rows: { type: ['string', 'null'] },
        },
        required: ['sheet', 'rows'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_print_scale',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the fixed print scale of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          scalePercent: { type: 'integer', minimum: 10, maximum: 400 },
        },
        required: ['sheet', 'scalePercent'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          scalePercent: { type: 'integer', minimum: 10, maximum: 400 },
        },
        required: ['sheet', 'scalePercent'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_protection',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the passwordless protection state of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          protected: { type: 'boolean' },
        },
        required: ['sheet', 'protected'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          protected: { type: 'boolean' },
        },
        required: ['sheet', 'protected'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: false,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.add',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Add an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: { name: { type: 'string' } },
        required: ['name'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: { name: { type: 'string' } },
        required: ['name'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.duplicate',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Duplicate one fully loaded XLSX worksheet to an explicit name.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          name: { type: 'string' },
        },
        required: ['sheet', 'name'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          name: { type: 'string' },
        },
        required: ['sheet', 'name'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: false,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_tab_color',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set or clear the explicit tab color of an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          color: { type: ['string', 'null'] },
        },
        required: ['sheet', 'color'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          color: { type: ['string', 'null'] },
        },
        required: ['sheet', 'color'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.set_visibility',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Set the explicit visible state of one XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          visible: { type: 'boolean' },
        },
        required: ['sheet', 'visible'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          visible: { type: 'boolean' },
        },
        required: ['sheet', 'visible'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.rename',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Rename an XLSX worksheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          name: { type: 'string' },
        },
        required: ['sheet', 'name'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          name: { type: 'string' },
        },
        required: ['sheet', 'name'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.delete',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Delete an XLSX worksheet while retaining at least one sheet.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: { sheet: { type: 'string' } },
        required: ['sheet'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: { sheet: { type: 'string' } },
        required: ['sheet'],
        additionalProperties: false,
      },
      risk: 'high',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.sheet.move',
      format: 'xlsx',
      family: 'sheet',
      summary: 'Move an XLSX worksheet to a 1-based tab position.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          position: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'position'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          sheet: { type: 'string' },
          position: { type: 'integer', minimum: 1 },
        },
        required: ['sheet', 'position'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.row.delete',
      format: 'xlsx',
      family: 'row',
      summary: 'Delete rows from a 1-based XLSX row position.',
      visibility: 'agent',
      inputSchema: xlsxRowMutationInputSchema,
      outputSchema: xlsxRowMutationOutputSchema,
      risk: 'high',
      context: ['document'],
      effects: ['document'],
      mutates: true,
      undoable: true,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.document.load_staged',
      format: 'xlsx',
      family: 'document',
      summary: 'Load staged XLSX bytes into the active editor.',
      visibility: 'internal',
      inputSchema: {
        type: 'object',
        properties: {
          blobId: { type: 'string' },
          name: { type: 'string' },
          size: { type: 'integer' },
          data: {
            type: 'object',
            description: 'Hydrated ArrayBuffer supplied by the live-session host bridge.',
            properties: {},
            required: [],
            additionalProperties: false,
          },
        },
        required: ['blobId', 'name', 'size', 'data'],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          opened: { type: 'boolean' },
          fileName: { type: 'string' },
        },
        required: ['opened', 'fileName'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['document', 'selection'],
      mutates: true,
      undoable: false,
      atomic: true,
      compatibilityAliases: [],
    },
    {
      id: 'xlsx.document.save',
      format: 'xlsx',
      family: 'document',
      summary: 'Save the active XLSX workbook.',
      visibility: 'agent',
      inputSchema: {
        type: 'object',
        properties: {},
        required: [],
        additionalProperties: false,
      },
      outputSchema: {
        type: 'object',
        properties: {
          saved: { type: 'boolean' },
          fileName: { type: 'string' },
        },
        required: ['saved', 'fileName'],
        additionalProperties: false,
      },
      risk: 'medium',
      context: ['document'],
      effects: ['persistence'],
      mutates: true,
      undoable: false,
      atomic: true,
      compatibilityAliases: [],
    },
  ],
} as const satisfies OperationCatalog

export default xlsxOperationCatalog
